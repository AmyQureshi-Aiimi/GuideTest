# SAR

Subject Access Requests can be configured in multiple ways. You can select a SAR import source, set the storage that is used for redacted documents, and configure which user groups have access to the feature.
