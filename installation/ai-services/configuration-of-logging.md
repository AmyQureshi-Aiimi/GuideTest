# Configuration of Logging

Logging configuration can be found in the config subfolder. The logging.conf file is a YAML file and follows a similar structure to log4j and log4net.

The out of the box configuration is suitable for most deployments, but you can change the log level settings to suit your needs.

By default, logs are written to the logs subfolder.
