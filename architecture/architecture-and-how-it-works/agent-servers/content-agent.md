# Content Agent

The content agent works with the source agent to proxy requests. It helps fetch data, caching, generating previews and generating thumbnails for documents.

Each server that hosts a source agent will have a content agent within the Control Hub. There are no options available for the content agent for now.
